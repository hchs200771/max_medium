<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%= link_to root_path, class: "navbar-item" do %>
      <img src="日向.png" width="112" height="28">
      <h1 class="title" style="color: blue; padding: 0 10px">Max's Blog</h1>
    <% end %>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          <% if user_signed_in? %>
            <div class="navbar-item has-dropdown is-hoverable">
              <a class="navbar-link">
                <%= content_tag :i, '', class: 'fas fa-crown has-test-primary' if current_user.vip_user? %>
                <%= content_tag :i, '', class: 'fas fa-crown has-test-danger' if current_user.platinum_user? %>
                <%= avatar(current_user) %>
                <%= current_user.username %> 你好
              </a>
              <div class="navbar-dropdown">
                <%=link_to "新增故事", new_story_path, class: "navbar-item"%>
                <%=link_to "故事列表", stories_path, class: "navbar-item"%>
                <hr class="navbar-divider">
                <%=link_to "會員升級", pricing_users_path, class: "navbar-item" unless current_user.paid_user? %>
                <%=link_to "個人資料", edit_user_registration_path, class: "navbar-item"%>
                <hr class="navbar-divider">
                <%=link_to "登出", destroy_user_session_path, class: "navbar-item", method: "delete", data: {confirm: "是否確定登出？"} %>
              </div>
            </div>

          <% else %>
            <%=link_to "註冊", new_user_registration_path, class: "button is-primary"%>
            <%=link_to "登入", new_user_session_path, class: "button is-light"%>
          <% end %>

        </div>
      </div>
    </div>
  </div>
</nav>
